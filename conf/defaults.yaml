config_file: null  # This lets the user pass a config filename to load other arguments from

program:  # These are the arguments that define how the train.py script works
  seed: 0
  run_name: test-run
  wandb_project: biomass
  output_root_dir: output
  output_dir: ${program.output_root_dir}/${program.run_name}
  log_dir: ${program.output_dir}/logs
  data_dir: data
  s3_output_uri: null
  overwrite: False
  clear_output_dir: False
  train: True
  plot_dataset_samples: 4
  predict: True
  plot_predictions: 8
  wandb: True
